<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h2>Editar Pet para Adoção</h2>
            <hr>

            <h4>Imagens Atuais</h4>
            <div class="row mb-4">
                <?php if (!empty($pet->images)): ?>
                    <?php foreach ($pet->images as $image): ?>
                        <div class="col-md-4 mb-3">
                            <div class="card">
                                <?php
                                $imageData = base64_encode($image->image);
                                $imageUrl = 'data:image/jpeg;base64,' . $imageData;
                                ?>
                                <img src="<?= $imageUrl ?>" class="card-img-top" style="height: 150px; object-fit: cover;">
                                <div class="card-body text-center">
                                    <form action="/pets/images/<?= $image->id ?>/delete" method="POST" onsubmit="return confirm('Tem certeza que deseja excluir esta imagem?');">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <button type="submit" class="btn btn-danger btn-sm">Excluir</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    <?php endforeach; ?>
                <?php else: ?>
                    <p class="text-muted">Nenhuma imagem cadastrada.</p>
                <?php endif; ?>
            </div>

            <hr>

            <form action="/pets/<?= $pet->id ?>/update" method="POST" enctype="multipart/form-data">

                <div class="mb-3">
                    <label for="pet_images" class="form-label">Adicionar Novas Imagens</label>
                    <input type="file" class="form-control" id="pet_images" name="pet_images[]" multiple>
                    <div class="form-text">Você pode selecionar várias imagens (jpg, jpeg, png).</div>
                </div>

                <?php 
                    $buttonText = 'Salvar Alterações';
                    include '_form.phtml'; 
                ?>
            </form>
        </div>
    </div>
</div>
