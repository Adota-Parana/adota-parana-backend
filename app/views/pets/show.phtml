<div class="container mt-5">
    <div class="row">
        <!-- COLUNA DA IMAGEM -->
        <div class="col-md-6">

        <?php if (!empty($pet->images)): ?>
            <div id="petImageCarousel" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-inner">
                    <?php foreach ($pet->images as $index => $image): ?>
                        <div class="carousel-item <?= $index === 0 ? 'active' : '' ?>">
                        <img 
                            src="/<?= htmlspecialchars($image->image_path) ?>"
                            class="d-block w-100 rounded"
                            alt="Foto do pet"
                            style="height: 400px; object-fit: cover;">
                        </div>
                    <?php endforeach; ?>
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#petImageCarousel" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Anterior</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#petImageCarousel" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Próximo</span>
                </button>
            </div>
        <?php else: ?>
            <img src="/assets/images/defaults/animals.jpeg" class="img-fluid rounded" style="height:400px; object-fit:cover;">
        <?php endif; ?>

        </div>

        <!-- COLUNA DE INFORMAÇÕES -->
        <div class="col-md-6">
            <h1><?= htmlspecialchars($pet->name) ?></h1>
            <hr>
            <p><strong>Descrição:</strong> <?= htmlspecialchars($pet->description) ?></p>
            <p><strong>Sexo:</strong> <?= htmlspecialchars($pet->sex) ?></p>
            <p><strong>Data de Nascimento:</strong> <?= htmlspecialchars($pet->birth_date) ?></p>
            <p><strong>Vacinado:</strong> <?= $pet->is_vaccinated ? 'Sim' : 'Não' ?></p>
            <p><strong>Castrado:</strong> <?= $pet->is_neutered ? 'Sim' : 'Não' ?></p>
            <hr>

            <a href="/adocao/<?= $pet->id ?>" class="btn btn-success btn-lg">Quero Adotar!</a>
        </div>
    </div>
</div>
